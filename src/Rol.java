public enum Rol {
    /**
     * Dado que solo hay 2 valores posibles (Alpinista y medico ) ,
     * es mas eficiente y claro implementa la clase rol como enumerado .
     *
     * Los enum son ideales para casos donde tienes un conjunto fijo
     * y limitado de valores constantes
     *
     * tambien con un enum puedes agregar nuevos roles facilmente
     * si es necesario, simplemente añadiendo un nuevo valor al enumerado.
      */

    ALPINISTA , MEDICO
}
